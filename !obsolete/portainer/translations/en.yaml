configuration:
  log_level:
    name: Log Level
    description: Choose the log level for the service (e.g., info, debug, warning, error).

network:
  9000/tcp: Web Interface
  8000/tcp: Agent Communication
