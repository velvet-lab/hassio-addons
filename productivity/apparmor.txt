#include <tunables/global>

profile productivity flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/nameservice>    # DNS‑Auflösung
  #include <abstractions/apache2-common> # enthält nützliche Regeln für Web‑Server

  # Capabilities
  file,
  signal (send) set=(kill,term,int,hup,cont),
  capability sys_ptrace,
  capability dac_override,
  capability net_bind_service,

  # S6-Overlay
  /init ix,
  /bin/** ix,
  /usr/bin/** ix,
  /run/{s6,s6-rc*,service}/** ix,
  /package/** ix,
  /command/** ix,
  /etc/services.d/** rwix,
  /etc/cont-init.d/** rwix,
  /etc/cont-finish.d/** rwix,
  /run/{,**} rwk,
  /dev/tty rw,

  # Bashio
  /usr/lib/bashio/** ix,
  /tmp/** rwk,

  # Add‑on Daten
  /data/** rw,

  # Memory‑Mapping & Proc
  /proc/** r,
  /proc/self/** r,
  /dev/shm/** mrwix,
  owner /** mrwix,               # erlaubt anonyme mmap/exec (vorsichtig!)
  /dev/urandom r,                # Entropiequelle

  # Executable und Bibliotheken
  /usr/sbin/nginx           rix,
  /usr/lib/nginx/**         rix,

  # Konfiguration
  /etc/nginx/**             r,

  # Document‑Root (hier: /var/www, ggf. anpassen)
  /opt/super-productivity/**               r,

  # Log‑Dateien (schreiben)
  /var/log/nginx/**         rw,

  # Cache‑Verzeichnis (falls genutzt)
  /var/cache/nginx/**       rw,

  # PID‑Datei und Unix‑Domain‑Sockets
  /run/nginx.pid            w,
  /run/nginx/**             rwk,

  # Temporäre Dateien (z. B. Upload‑Verarbeitung)
  /tmp/**                   rwk,

  # System‑ und Proc‑Informationen (Read‑Only)
  /proc/**                  r,
  /proc/self/**             r,
  /dev/urandom              r,

  /* --------------------------------------------------------------
    * Signal‑Handling
    * -------------------------------------------------------------- */
  signal (receive) peer=nginx,
  signal (send)    set=(term,quit,hup)
}